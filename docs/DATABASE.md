# Database

PostgreSQL 16 + SQLAlchemy 2 + Alembic.

## Phase 1 tables

- `users` (shell)
- `projects`
- `videos` (shell for Phase 2)
- `processing_jobs`

Later phases add transcript, candidate, clip, edit plan, render tables via new migrations.
