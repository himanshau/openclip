# API

## Phase 1

| Method | Path | Description |
|--------|------|-------------|
| GET | `/health` | App + DB + Redis |
| GET | `/api/system/status` | GPU, RAM, disk, FFmpeg, models, workers |

Domain routes (projects, videos, clips, …) arrive in later phases.
